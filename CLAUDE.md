# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

Infrastructure-as-Code for a personal homelab. All services run as Docker Compose stacks inside of Portainer, grouped by host machine. There is no build system, CI pipeline, or test suite. Each compose file corresponds to a "stack" in Portainer.

**IaC migration is in progress.** The goal is to have Portainer deploy all stacks directly from this Git repo (GitOps mode) rather than managing them via the Portainer web editor. See the IaC Migration Status section below.

## Repository layout

```
stacks/
  nelson-nuc/   # Intel NUC, primary host — most services live here
  quark-vm/     # Proxmox VM (Tailscale IP 100.76.105.3) — paperless, crashplan, dozzle-agent
```

Each service is a subdirectory containing a `docker-compose.yml` and optionally a `.env-example` or `secrets/` example file.

## Architecture patterns

**Reverse proxy:** All services route through Traefik v3 (`stacks/nelson-nuc/traefik/`). Services expose themselves via Docker labels; non-Docker services (Proxmox, Portainer, router, NAS) are registered as static upstreams in `traefik/config.yml`. The shared external Docker network is named `proxy` — every container that needs Traefik routing must join it.

**TLS:** Wildcard certs via Cloudflare DNS challenge. The cert resolver is named `cloudflare`. Local services use `*.local.nelsonhickman.com`; internet-facing services use `*.nelsonhickman.com`.

**Secrets:** Two patterns are used:
- Docker secrets (`secrets:` block with an absolute path to a file on the host, e.g. `/home/nelson/containers/traefik/cf_api_token.txt`) — used for traefik and nextcloud
- `env_file` with an absolute host path (e.g. `env_file: /home/nelson/containers/mealie/.env`) — used when the upstream image expects env vars

Always use absolute paths for both — relative paths break when Portainer deploys from a Git clone. Secret files are never committed; `.env-example` and `secrets/*-example` files document what values are needed.

**Image versions:** All images are pinned to explicit versions (never `latest`), e.g. `traefik:v3.0`, `postgres:16`, `nextcloud:31-apache`. Two images intentionally stay on `latest` because they publish no versioned tags: `ghcr.io/vert-sh/vert` and `peco602/ansible-linux-docker`.

**Volumes:** Named Docker volumes for stateful data; bind mounts under `/home/nelson/containers/<stack>/` on nelson-nuc, and `/srv/<stack>/` on quark-vm.

**Timezone:** All containers must include `TZ=America/Los_Angeles` in their `environment` block.

## Adding a new service

1. Create `stacks/<host>/<service-name>/docker-compose.yml`
2. Join the `proxy` network (external: true)
3. Add Traefik labels for routing and TLS — use an existing stack as a template
4. If the service needs secrets, create a `secrets/` directory with `*-example` placeholder files
5. If the service needs env vars beyond what labels cover, create a `.env-example` and place the real `.env` at `/home/nelson/containers/<service>/.env` on nelson-nuc
6. For non-Docker upstreams (host IPs, Tailscale IPs), add a router + service entry to `stacks/nelson-nuc/traefik/config.yml`
7. Use absolute paths for all volume mounts, env_file references, and secret files
8. Set `TZ=America/Los_Angeles` in the `environment` block

## IaC Migration Status

**What runs manually (will always be bootstrapped by hand):**
- Portainer itself — must exist before it can manage anything
- The `proxy` Docker network — must be created on the host before any stack deploys (`docker network create proxy`)

**Phase 1 — complete:** All image versions pinned; relative `env_file: .env` references removed; compose files audited for absolute volume paths.

**Phase 2 — complete:** Secrets strategy settled (absolute-path env_file + Docker secrets). Traefik compose was truncated in the repo and has been restored. Nextcloud secret path fixed to absolute. Portainer now has `/home/nelson/containers:/home/nelson/containers:ro` mounted into its own container (recreated 2026-08-22, confirmed via `docker inspect`), resolving the blocker that kept it from seeing absolute-path `env_file:` targets. Mealie (`env_file: /home/nelson/containers/mealie/.env`) and cloudflared (`env_file: /home/nelson/containers/cloudflared/.env`) have both been retested via live GitOps redeploys and confirmed working — mealie via clean logs and normal request traffic, cloudflared via a zero-downtime dual-connector cutover (temporarily renamed to `cloudflared-git` to run alongside the old container, verified healthy, then reverted — the revert redeploy also confirmed Portainer correctly re-pulls and applies upstream repo changes, not just initial deploys).

`traefik` is now fully done (2026-08-22): the `TRAEFIK_DASHBOARD_CREDENTIALS` label required a `stack.env` committed to the repo for Repository-mode GitOps, not viable in a public repo, so dashboard basic-auth was moved off the label into `config.yml`'s file-provider middleware (`traefik-auth: basicAuth.usersFile: /run/secrets/traefik_dashboard_htpasswd`), backed by a new Docker secret (same pattern as `cf_api_token`). Host-side files (`dashboard_htpasswd.txt`, synced `config.yml`) were created/updated on nelson-nuc, then redeployed and confirmed via Portainer — see Phase 3 below. Along the way, also fixed a `.gitignore` bug where `**/secrets/` excluded the whole directory and silently defeated the `!**/secrets/*-example` negation underneath it — NextCloud's example secret file had never actually been committed as a result; now fixed (`**/secrets/*`) and both traefik's and NextCloud's `*-example` files are tracked.

`reactive-resume` is now fully done too (2026-08-22, see `stacks/nelson-nuc/reactive-resume/FIXME.md` Issue 3): `${VAR}` substitution across services replaced with a single shared `env_file: /home/nelson/containers/reactive-resume/.env` (same pattern as adventurelog); composed values (`DATABASE_URL`, `STORAGE_ACCESS_KEY`/`STORAGE_SECRET_KEY`) written out literally since `env_file` can't interpolate. `CHROME_TOKEN`/`TOKEN` no longer exist in this stack (removed earlier per FIXME Issue 2 — unrelated to this pass). Host `.env` created at `/home/nelson/containers/reactive-resume/.env` with live values pulled via `docker inspect` on the running containers (the old on-disk `.env` under `/home/nelson/stacks/reactive-resume/` was stale — `AUTH_SECRET`/`MAIL_FROM` were actually set via Portainer's env var UI, not that file).

**Near-miss caught during this pass, worth remembering:** the stack was connected to GitOps *before* the fix commit was pushed — Portainer pulled the then-current commit (still `${VAR}`-based, no `.env` in the Git clone dir), so every substitution silently resolved to an empty string. `postgres` looked "healthy" only because it was reusing its existing data volume (init scripts don't rerun); the app itself crash-looped on "Invalid environment variables" and the site was down. Fixed by pushing the already-written fix (`e11887c`) and re-pulling in Portainer; confirmed via `docker inspect` (real secret values now present), clean `docker logs`, and a `200` from `https://resume.local.nelsonhickman.com/`. **Lesson for future stacks:** commit and push repo-side compose/env fixes *before* flipping GitOps on (or before the next redeploy), not after — this repo's `${VAR}`-substitution issue only surfaces as silent empty strings, not a deploy failure, so it's easy to miss.

**Phase 3 — in progress:** Connect each stack to Portainer GitOps (repo URL `https://github.com/nph4/Homelab-IaC`, branch `main`). No PAT is configured or needed — the repo is public, so anonymous HTTPS pulls work fine (only relevant if the repo goes private, or anonymous rate limits become an issue). Done: `mealie`, `cloudflared`, `traefik` (2026-08-22 — redeployed via Portainer GitOps at commit `1c8a46b`, `docker inspect` confirms `com.docker.compose.project.config_files` pointing at the Portainer-managed Git clone, pinned `v3.0.4`, dashboard auth now `traefik-auth@file` with no leftover label-based basicauth, clean logs), `reactive-resume` (2026-08-22 — see Phase 2 note above for the near-miss hit along the way; confirmed healthy at commit `e11887c`), `it-tools` (2026-08-22 — first-ever deploy of this stack, no prior manually-managed container to migrate; confirmed via `docker inspect` sourced from the Git clone, `TZ` set, clean logs, `200` through Traefik on `it-tools.local.nelsonhickman.com`). `vert` (2026-08-22 — repo-side fixed first at commit `2e853e6`, filename normalized to `.yml` and missing `TZ` added, then confirmed via `docker inspect` sourced from the Git clone at `c2b6f94`, `healthy`, `TZ` set, clean logs, `200` through Traefik). `homebox` (2026-08-22 — no repo changes needed, only pre-existing drift was a missing `TZ` on the then-running container, which the GitOps redeploy corrected; confirmed via `docker inspect` sourced from the Git clone, `healthy`, `TZ` set, existing SQLite data preserved across redeploy, clean logs, `200` through Traefik). `dashy` (2026-08-22 — repo's tracked config was stale/misnamed: `myconfig.yml` didn't match the actual host bind-mount filename `my-config.yml` and was missing a "Days Since Incident" entry; `.env-example` was mislabeled and held an even older config snapshot, not env vars. Both replaced with a single `my-config.yml` synced from the live host file — commit `f613b15`. Confirmed `healthy`, sourced from the Git clone, bind mount intact, `200` through Traefik). Note: traefik's `config.yml`/`traefik.yml`/`acme.json` are still bind-mounted from `/home/nelson/containers/traefik/data/` on the host rather than sourced from the Git clone directly — future repo changes to `config.yml` still need manual host sync (see Phase 2 note above). `uptime-kuma` is now fully done (2026-08-22): its data volume was created under the old Portainer project name `uptime_kuma` (underscore), so the live volume is literally named `uptime_kuma_uptime-kuma` and holds a real 350MB `kuma.db` (monitor history back to Dec 2023). The repo's plain `uptime-kuma:` volume declaration would have gotten re-prefixed under the new GitOps stack's name (almost certainly `uptime-kuma`, hyphen), resolving to a volume that doesn't exist and silently creating an empty one on first deploy. Fixed by pinning `volumes.uptime-kuma` to `name: uptime_kuma_uptime-kuma` with `external: true`. Confirmed after redeploy: sourced from the Git clone, `healthy`, mount correctly attached to the existing volume (not a fresh one), `kuma.db` byte-identical in size to before, clean startup logs, `302` to `/dashboard` through Traefik. **Worth checking other not-yet-migrated stacks with named volumes for the same landmine before connecting them to GitOps** — this is a second instance (after reactive-resume) of a real host state that isn't visible just from reading the repo's compose file.

`dozzle` (2026-08-22 — no repo changes needed, no volumes to worry about; confirmed sourced from the Git clone, `DOZZLE_REMOTE_AGENT` unchanged, startup log shows `"clients":2` (both the nelson-nuc and quark-vm agents connected), `200` through Traefik). `dozzle-agent` on nelson-nuc (2026-08-22 — no repo changes needed; confirmed sourced from the Git clone, `TZ` now set, docker.sock RO mount intact, listening on `7007`, dozzle main reconnected cleanly with no errors). quark-vm's `dozzle-agent` also done (2026-08-22 — no repo changes needed; confirmed via commands run on quark-vm directly (no SSH access to that host from this session/agent), sourced from the Git clone, `TZ` set, docker.sock RO mount intact, listening on `7007`, and dozzle main still showed `"clients":2` with no errors after the restart). `wallos` is now fully done (2026-08-22): compose had `ghcr.io/ellite/wallos:v4.6.2` — that tag doesn't exist upstream (`docker pull` returns `manifest unknown`); the real published/running tag is `4.6.2` (no `v`). Would have failed outright at image pull on GitOps deploy. Fixed by dropping the stray `v`; confirmed after redeploy sourced from the Git clone, `healthy`, existing `wallos-db`/`wallos-logos` volumes correctly reused ("Database already exist... No migrations to run"), `302` to `/login.php` through Traefik.

`calibre-web` (2026-08-22 — no repo changes needed. Bind mounts live under `/srv/containers/calibre/...` on nelson-nuc rather than `/home/nelson/containers/calibre-web/...` like CLAUDE.md's usual convention — pre-existing, paths are valid/populated, not worth touching for real book/library data. First redeploy took several minutes due to the `universal-calibre` DOCKER_MODS apt install of Calibre's Qt/X11 dependency chain — not an error, just slow; confirmed via `RestartCount: 0` it wasn't crash-looping. Confirmed sourced from the Git clone, `TZ` corrected, all three bind mounts intact, clean finish log, `302` to `/login` through Traefik). `jellyfin` (2026-08-22 — no repo changes needed, zero drift found even before redeploy; confirmed sourced from the Git clone, GPU passthrough intact (`vaapi`/`qsv` hwaccel present in ffmpeg's encoder/decoder list, `/dev/dri` + render group `109` correctly applied), existing `ServerId` preserved so config/library data carried over rather than a fresh install, all three media libraries mounted and watched, `302` to `/web/` through Traefik). `adventurelog` (2026-08-22 — first-ever deploy of this stack, no prior manually-managed container to migrate, same category as `it-tools`. Its `env_file` target `/home/nelson/containers/adventurelog/.env` didn't exist on the host yet — created it with a freshly generated `POSTGRES_PASSWORD` before deploy, since a missing `env_file` is a hard failure, not a silent fallback like `${VAR}` substitution. Deploy itself took a while: Portainer's own image pull for `postgis/postgis:16-3.5` stalled on a flaky layer retry — resolved by pulling the same image directly via `docker pull` on the host, after which Portainer's compose picked it up immediately. First boot then ran ~10 minutes of one-time seeding (Django migrations, ~250 country flag downloads, then 250 countries/5322 regions/153728 cities loaded into Postgres) — not an error, just slow; confirmed via `RestartCount: 0` it wasn't crash-looping. `adventurelog.local.nelsonhickman.com` briefly failed to resolve on nelson-nuc right after deploy (compared against a working host to confirm this isn't wildcard DNS, each hostname is its own record) but self-resolved shortly after, likely propagation/negative-cache delay — no manual DNS action ended up being needed. Confirmed sourced from the Git clone, both containers `healthy`, `200` through Traefik).

**Security follow-up on adventurelog (2026-08-22, unrelated to GitOps mechanics but found immediately after this deploy):** the image silently defaults to `DJANGO_ADMIN_USERNAME=admin` / `DJANGO_ADMIN_PASSWORD=admin` / `DJANGO_ADMIN_EMAIL=admin@example.com` (set in its own `/aio/env-setup.sh`) whenever those vars aren't provided — so a superuser with the literal password `admin` was live on this publicly-exposed instance. Also, public signup was open by default. Fixed: rotated the `admin` account's password immediately via `manage.py shell` (didn't require a redeploy), promoted the real `nelson` account to superuser, then added `DISABLE_REGISTRATION=true` and pinned strong `DJANGO_ADMIN_*` values to `/home/nelson/containers/adventurelog/.env` (the latter only matters for a future from-scratch DB bootstrap, since `create_superuser_if_needed` no-ops once the account exists) — this part needed a container recreate to take effect, since `DISABLE_REGISTRATION` is read once at Django settings load. Confirmed after redeploy: both `admin`/`nelson` accounts and their superuser status survived (Postgres volume untouched), `/auth/is-registration-disabled/` returns `{"is_disabled":true}`. **Worth checking whether any other not-yet-deployed stack has a similar "insecure-by-default until you override it" env var** — this is exactly the kind of thing that doesn't show up just from reading the compose file, only from reading the image's own entrypoint scripts.

`unifi` (2026-08-22 — no repo changes needed. Live container was running `jacobalberty/unifi:latest`, but its digest was confirmed byte-identical to the repo's pinned `v10.0.162`, so switching tags was a non-event, not a version change. Its `/unifi/run` volume was anonymous live vs. the repo's named `unifi-run` — confirmed low-risk beforehand (only transient runtime state) and indeed just got created fresh (`unifi_unifi-run`) with no ill effect. Its Traefik routing is static, via `config.yml`'s host-IP upstream (`https://192.168.88.101:8443`), not Docker labels — so it was never affected by the GitOps switch either way. Confirmed after redeploy: sourced from the Git clone, controller fully up, and its persistent `uuid` in `/status` is identical to before the redeploy — definitive proof the bind-mounted sites/devices/history data survived untouched, `302` through Traefik). `home-assistant` — **repo-side fix done (2026-08-22), not yet redeployed.** Compose pinned `2026.5.2`, but the live container (running the floating `:stable` tag, never recreated since first pull) is actually on `2024.5.1` — checked via `homeassistant.const.__version__` inside the container, ~2 years / ~24 releases behind. Unlike a routine GitOps mechanical fix, this would have been a real HA upgrade with real risk (breaking integrations, deprecated YAML, no clean rollback) sprung on the user just by connecting GitOps. Asked the user how to handle it: chose to re-pin the repo to `2024.5.1` (confirmed byte-identical digest to what's running — a true no-op) and defer the actual 2024.5.1→2026.5.2 upgrade to its own deliberate, backed-up step later, matching how every other stack in this migration has been handled (GitOps connection ≠ silent version bump). Also corrected a stale comment (`/dev/serial/by-id` device is a Zigbee dongle, not Z-Wave as previously commented). Only other drift found: missing `TZ` on the live container, same harmless self-correcting pattern as several earlier stacks.

Suggested order for the rest: nextcloud → days-since-incident.

**Pre-checked while looking for more `uptime-kuma`-style volume landmines (2026-08-22), findings for stacks still ahead in the queue:**
- `days-since-incident`, `nextcloud`, `wallos` — safe as-is. Live volumes already use hyphenated names matching their repo directory names (e.g. `wallos_wallos-db`), so a new GitOps stack named consistently with the repo folder will reuse them correctly. No fix needed.
- `adventurelog` — not deployed on the host at all yet (no container, no volumes) — will be a fresh install like `it-tools` was, nothing to lose.
- `unifi` — **needs a look when we get to it, but risk is low, not another uptime-kuma.** Its main data (`/unifi`, the actual controller config/db) is already a bind mount at `/home/nelson/unifi` and matches the repo's `docker-compose.yml` exactly — no risk there. The repo also declares a named volume `unifi-run` for `/unifi/run`, but the *live* container is actually using an anonymous (unnamed, hash-named) volume for that mount instead — meaning it was never deployed with the repo's current compose. Checked what's actually in it: just `mongod.pid`, a `mongodb-*.sock`, old `packet-capture*` debug dirs (going back to 2023), and a `support.encrypted` bundle — all transient runtime/debug state, not the real controller database. So letting GitOps create a fresh named `unifi-run` volume on first deploy should be harmless (the container recreates this directory's contents on boot regardless) — just confirm post-deploy that the controller still has its sites/devices/history intact (that lives on the bind mount, unaffected either way) before calling it done.

**Phase 4 — not started:** Set up GitHub webhooks pointing at Portainer per-stack webhook URLs for automatic redeployment on push.
